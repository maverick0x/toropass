name: toropass_client
description: "Flutter client package for launching ToroPass Wallet OAuth identity verification flows."
version: 0.1.1
homepage: https://github.com/maverick0x/toropass/tree/main/packages/toropass_client
repository: https://github.com/maverick0x/toropass
issue_tracker: https://github.com/maverick0x/toropass/issues
topics:
  - oauth
  - identity
  - deeplink
  - wallet
  - authentication

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  app_links: ^6.4.1
  flutter:
    sdk: flutter
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
